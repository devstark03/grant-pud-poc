# Deploy ADF Resources
#
# This workflow will eventually deploy Azure Data Factory ARM templates
# using the Azure CLI. It will:
# 1. Authenticate to Azure via a service principal (OIDC)
# 2. Validate ARM templates against the target resource group
# 3. Deploy linked services, datasets, pipelines, and triggers
# 4. Start any scheduled triggers after deployment

name: Deploy ADF

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy ADF
        run: echo "TODO: deploy ADF resources"
