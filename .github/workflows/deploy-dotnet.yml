# Deploy .NET Monitoring Service
#
# This workflow will eventually build and deploy the PipelineMonitor
# service to Azure App Service. It will:
# 1. Build the .NET 8 solution
# 2. Run tests
# 3. Publish the Worker and API projects
# 4. Deploy to Azure App Service using the Azure CLI

name: Deploy .NET Service

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy .NET Service
        run: echo "TODO: build and deploy .NET monitoring service to Azure App Service"
